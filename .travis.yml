sudo: required
language: generic

services:
  - docker

script:
  - wget -O- https://github.com/harisvsulaiman/travis.debian.net/blob/gh-pages/script.sh | sh -

branches:
  except:
    - /^debian\/\d/